# Models

Mongoose models, one file per collection, PascalCase (`Player.js`, `LedgerEntry.js`, …).
Phase 7 adds `players`, `ledger`, `purchases`, `grants`, `codes`, `attempts`, `config`
(docs/DESIGN.md §11 Backend).
